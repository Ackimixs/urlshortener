{
  "name": "testingexpress",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "@types/express": "^4.17.17",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "start": "node dist/index.js",
    "build": "npx tsc",
    "postbuild": "cp -r src/public dist/public && echo 'Done.'",
    "dev": "npm run clean && npm run build && npm run start",
    "prisma:migrate:dev": "npx prisma migrate dev",
    "prisma:migrate:generate": "npx prisma generate",
    "prisma:studio": "npx prisma studio --browser none",
    "prisma:studio:browser": "npx prisma studio"
  }
}
