{
  "name": "testingexpress",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "5.2.0",
    "@types/express": "^4.17.17",
    "express": "^4.18.2",
    "prisma": "^5.2.0"
  },
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "start": "bun server/index.ts",
    "prisma:migrate:dev": "bunx prisma migrate dev",
    "prisma:migrate:generate": "bunx prisma generate",
    "prisma:studio": "bunx prisma studio --browser none",
    "prisma:studio:browser": "bunx prisma studio"
  }
}
